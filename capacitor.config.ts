import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.storepricelist.app',
  appName: 'Store Price List',
  webDir: 'dist'
};

export default config;
